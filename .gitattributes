* text=auto eol=lf

# Source-feasibility golden bundles are captured evidence bytes and must
# remain byte-identical across platforms; never normalize line endings.
data/source-feasibility/** -text -whitespace

# Security-lineage evidence receipts are byte-pinned development evidence.
data/security-lineage/evidence/** -text -whitespace
